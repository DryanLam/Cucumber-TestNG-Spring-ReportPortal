# Initialize driver information
browser.test.type = CHROME
remote.server.hub =
#remote.server.hub = http://localhost:4444
browser.script.timeout = 60
browser.page.timeout = 60

# Folder of test data
data.test.folder = /src/test/groovy/com/sample/dl/bdd/data

# Only support INFO, OFF
log.level = INFO

# Database connection information
db.connect.driver=org.postgresql.Driver

#db.connect.url=jdbc:postgresql://127.0.0.1:5432/school

db.connect.url=jdbc:postgresql://192.168.1.117:5432/sonar

db.connect.dbuser=sonar

db.connect.dbpassword=sonar